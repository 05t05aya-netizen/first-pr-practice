# first-pr-practice

A tiny sample project with a couple of useful helper functions.

## Functions

- `add(a, b)` - adds two numbers
- `greet(name)` - returns a greeting string

## Usage

```bash
python main.py
```

## Tests

```bash
python -m unittest discover tests
```
