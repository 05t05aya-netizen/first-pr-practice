# CLAUDE.md

このリポジトリで作業するときの前提と規約。

## このリポジトリは何か

株式投資の学習ノートを置いている個人用リポジトリ。もともとは PR を出す練習用だった。

| ファイル | 内容 |
| --- | --- |
| `docs/stock-basics.md` | 本体。15章の学習ノート |
| `stock_tools.py` | ノート内の計算式を関数にしたもの |
| `main.py` | 練習用の残骸（`add` と `greet`）。ノートとは無関係 |

読者は**投資を始めたばかりの学生・少額投資家**が想定。専門家向けではない。

## ドキュメントの規約

`docs/stock-basics.md` を編集するときは、以下を守る。

**1. 用語は「定義 → 具体例（数字入り） → たとえ」の順で説明する**

これが全体の一貫した方針。定義だけで終わらせない。具体例には必ず**実際の数字**を入れ、たとえは日常のものに置き換える（ピザ、財布、屋台、雪だるま、コインパーキングなど）。

**2. 企業名・数値はすべて架空**

実在企業の業績を断定しない。「架空のパン屋チェーン」のように明示する。

**3. 制度・税制は「確認してください」を添える**

NISA の枠、税率、扶養の判定ラインなどは改正される。断定せず、一次情報（国税庁・金融庁・証券会社）で確認するよう促す。

**4. 章番号を変えたら3か所を直す**

- 冒頭の目次（アンカーリンクも）
- 見出しの番号
- 本文中の相互参照（「◯章参照」）

**5. 免責を消さない**

冒頭の免責ブロックは、特定銘柄の推奨ではないことを明示している。必ず残す。

## `stock_tools.py` の規約

- docstring は日本語。**指標の意味とたとえ**を1〜2行入れる（ノートと同じ説明を再掲する）
- 各関数に **doctest** を付ける。数値例はノート本文と一致させる
- 変更したら `python3 -m doctest stock_tools.py -v | tail -3` で通ることを確認する
- 依存ライブラリを増やさない（標準ライブラリのみ）

## 数値を書くときは検算する

複利・積立・税額などの数字は、書く前に実際に計算する。過去に信託報酬の比較表で誤りがあり、再計算して修正した。

```bash
python3 -c "print(100*(1.049)**30)"
```

## Web 版

同じ内容を読みやすくした HTML 版を Artifact として公開している。
`docs/stock-basics.md` を大きく変更したら、Web 版も同じ内容に更新する（章番号のずれに注意）。

## 個人的な運用メモについて

**このリポジトリは public。** 保有銘柄・金額・口座・証券会社などの個人的な情報は、
コミットせず `CLAUDE.local.md`（`.gitignore` 済み）に書く。

そちらに現在の保有状況・決めたルール・次にやることが書いてある。
セッション開始時に両方読み込まれるので、文脈はそこから引き継げる。

## コミットの慣習

- コミットメッセージは日本語。1行目は `docs:` や `feat:` などの接頭辞を付ける
- 本文には**何を変えたかだけでなく、なぜそう変えたか**を書く
- 作業ブランチは `claude/...`。`main` へ直接コミットしない
